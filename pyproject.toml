[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-evaluation-harness"
version = "0.1.0"
description = "Open-source framework for evaluating AI-powered systems"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
